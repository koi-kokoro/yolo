numpy>=1.24
Pillow>=9.5
PyYAML>=6
# Deployment export/validation dependencies. Install these into the existing training
# environment; PyTorch/CUDA are intentionally not pinned here.
onnx>=1.16,<2
onnxruntime>=1.18,<2
# Training/export uses the project's confirmed external Ultralytics environment.
ultralytics>=8.4.89
